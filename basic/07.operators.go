package main

import "fmt"

func main() {
	var x int = 20
	var y int = 30

	z := x * y
	fmt.Println(z)
}

/*
 * Arithmetic Operators: + - * / % ++ --
 * Relational Operators: == != > < >= <=
 * Logical Operators: && || !
 * Bitwise Operators: & | ^ << >>
 * Assignment Operators: = += -= *= /= %= <<= >>= &= ^= |=
 * Miscellaneous Operators: & *
 */
