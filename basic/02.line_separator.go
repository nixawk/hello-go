package main

import "fmt"

func main() {
        fmt.Println("Hello, World !")
        fmt.Println("Learning go !")
}

/*
 * In a Go program, the line separator key is a statement terminator. That is,
 * individual statements don't need a special separator like ";" in C. The Go
 * compiler internally places ";" as the statement terminator to indicate the
 * end of one logical entity.
 */
