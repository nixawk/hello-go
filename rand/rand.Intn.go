package main

import "math/rand"
import "fmt"

func main() {
        fmt.Println(rand.Intn(100))
        fmt.Println(rand.Intn(100))
}

/* Go’s math/rand package provides pseudorandom number generation. */
